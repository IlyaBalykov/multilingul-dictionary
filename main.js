const handler = require('./requestHandler.js')
const input = document.getElementById('translate-from-gua-word')

input.addEventListener("input", () => parseWord())
async function parseWord(){
    const xhr = new XMLHttpRequest()

}
